# Core
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
xgboost>=2.0
pyyaml>=6.0
joblib>=1.3

# LLM agent backend — the core summary agent (default). Pick a provider via
# agent.llm.provider.
google-genai>=1.0   # gemini provider (default; needs GEMINI_API_KEY/GOOGLE_API_KEY)
anthropic>=0.40     # claude provider (needs ANTHROPIC_API_KEY)
# openai>=1.40      # openai provider (needs OPENAI_API_KEY); `pip install -e .[openai]`

# Optional explainability (graceful fallback if absent)
# shap>=0.44

# Dev / test
pytest>=8.0
